# Image Directory

This directory contains publication-style demo outputs:

- `acceleration_plot.png`
- `dis_freq_matrix.png`
- `prediction.png`

Regenerate them with:

```bash
python3 scripts/test_matrix.py demo --trace-index 3 --save-figures
```

Use `img/generated/` for local exploratory plots that should not be committed.
