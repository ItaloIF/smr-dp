# Models Directory

This directory stores model artifacts needed by the demo and inference scripts.

Included checkpoint:

```text
HP_DeepLabV3Plus/best_model.pt
```

The checkpoint is loaded by `src/model.py`. Additional experiments should be
stored outside Git or attached to versioned releases when they become large.
